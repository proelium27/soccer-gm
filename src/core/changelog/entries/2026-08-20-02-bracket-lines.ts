import type { ChangelogEntry } from "../types.js";

const entry: ChangelogEntry = {
  date: "2026-08-20",
  title: "The lines in the cup brackets join things up now",
  items: [
    "When I redrew the brackets I said each tie sat between the two that feed it, with lines joining them. The lines were mostly wrong. A tie carrying an extra line under its score, a penalty shootout say, was taller than the ones beside it, and that pushed every round after it out of step with the ties it came from. The lines themselves were floating in the gap between columns, not touching the cards on either side. Both are fixed, so a bracket now reads like a bracket in the Continental Cup and the Shield.",
  ],
};

export default entry;
